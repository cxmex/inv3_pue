name: Android Build

on:
  push:
    paths:
      - 'android/**'
      - '.github/workflows/android-build.yml'
  workflow_dispatch: {}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Accept SDK licenses + install platform/build-tools
        run: |
          yes | "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" --licenses > /dev/null || true
          "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" "platforms;android-34" "build-tools;34.0.0"

      - name: Build debug APKs
        working-directory: android
        run: gradle :app:assembleDebug :doublex1:assembleDebug --no-daemon --stacktrace

      - name: Upload terex3 APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: terex3-pos-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error

      - name: Upload doublex1 APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: doublex1-debug-apk
          path: android/doublex1/build/outputs/apk/debug/doublex1-debug.apk
          if-no-files-found: error
